import React from "react";
import LogoutButton from "../../auth/components/LogoutButton";

const home = () => {
    return (
        <div>
            <LogoutButton />
        </div>
    );
};

export default home;

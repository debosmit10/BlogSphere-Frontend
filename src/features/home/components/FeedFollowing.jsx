import React from "react";

const FeedFollowing = () => {
    return (
        <div>
            <span>Feed Following...</span>
            {/* Blogs of people the user is following will show up here */}
        </div>
    );
};

export default FeedFollowing;
